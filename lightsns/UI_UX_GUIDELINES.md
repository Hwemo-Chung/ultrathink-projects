# LightSNS UI/UX Design Guidelines
## 저대역폭 환경을 위한 초경량 SNS 디자인 원칙

**Version**: 1.0.0
**Last Updated**: 2025-10-28
**Target**: 2.2억 저대역폭 사용자 (< 1Mbps)

---

## 🎯 핵심 디자인 철학

### 1. Offline-First (오프라인 우선)
```
사용자는 인터넷 연결 없이도 앱을 사용할 수 있어야 합니다.
네트워크는 보너스이지, 필수가 아닙니다.
```

### 2. Minimal Data Usage (최소 데이터 사용)
```
모든 UI 요소는 데이터 사용량을 고려하여 설계됩니다.
화려함보다 효율성, 복잡함보다 단순함.
```

### 3. Battery Efficiency (배터리 효율)
```
어두운 색상, 적은 애니메이션, 효율적인 렌더링.
사용자의 배터리는 소중합니다.
```

### 4. Low-End Device Support (저사양 기기 지원)
```
512MB RAM, 오래된 프로세서에서도 부드럽게 작동.
최신 기기만을 위한 디자인은 NO.
```

---

## 🎨 핵심 UI 컴포넌트

### 1. Connection Status Indicator (연결 상태 표시)

**최우선 UI 요소** - 항상 표시되어야 함

#### Design Specs:
```
┌─────────────────────────────────────┐
│  🟢 Online  │  🔴 Offline  │ 🟡 Syncing │
│                                       │
│  상태바 상단 고정 (3-4px height)         │
│  색상으로 상태 명확히 구분                │
│  애니메이션 최소화 (배터리)               │
└─────────────────────────────────────┘
```

#### States:
- **🟢 Online**: 녹색, "온라인"
- **🔴 Offline**: 빨간색, "오프라인"
- **🟡 Syncing**: 노란색, "동기화 중..." (진행률 표시)
- **⚠️ Error**: 주황색, "동기화 실패"

#### Implementation:
```javascript
// 상태에 따라 UI 자동 변경
ConnectionStatus.onChange((status) => {
  switch(status) {
    case 'online':
      showBanner('🟢 온라인', 'success');
      startSync();
      break;
    case 'offline':
      showBanner('🔴 오프라인 모드', 'warning');
      enableOfflineMode();
      break;
    case 'syncing':
      showProgress('🟡 동기화 중...', progress);
      break;
  }
});
```

---

### 2. Emoji Reaction Bar (이모티콘 반응 바)

**댓글 대신 핵심 소통 수단**

#### Design Specs:
```
┌─────────────────────────────────────────┐
│  Post Content...                        │
│  [Image Preview]                        │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  👍   ❤️   😂   😮   😢   👏   │   │
│  │  42   18    7    3    1   12   │   │
│  └─────────────────────────────────┘   │
│                                         │
│  내 반응: 👍 (탭하면 취소)                 │
└─────────────────────────────────────────┘
```

#### Interaction Flow:
1. **탭 1회** → 반응 추가 (즉시 로컬 업데이트)
2. **탭 2회** → 반응 취소
3. **애니메이션**: 미니멀 (배터리 고려)
4. **햅틱 피드백**: 가볍게 (선택적)

#### UI Rules:
- 6가지 이모티콘 고정 (추가 옵션 없음)
- 큰 터치 영역 (44x44pt 최소)
- 내 반응은 하이라이트 (투명도 100%)
- 다른 반응은 약간 흐림 (투명도 60%)
- 숫자는 압축 표시 (1K, 10K 등)

#### Data Size:
```json
// API 응답 (압축)
{
  "r": {
    "0": 42,   // 👍
    "1": 18,   // ❤️
    "2": 7,    // 😂
    "3": 3,    // 😮
    "4": 1,    // 😢
    "5": 12    // 👏
  },
  "u": 0  // 내 반응 (0 = 👍)
}
// Size: ~40 bytes
```

---

### 3. Post Feed (게시물 피드)

**Virtual Scrolling + Lazy Loading**

#### Design Pattern:
```
┌───────────────────────────────────────┐
│  ┌─────────────────────────────────┐ │
│  │  @username  • 2시간 전           │ │ ← 50px
│  ├─────────────────────────────────┤ │
│  │                                 │ │
│  │  [Image Placeholder]            │ │ ← 250px
│  │  → 스크롤 시 로딩                  │ │
│  │                                 │ │
│  ├─────────────────────────────────┤ │
│  │  Post content text...           │ │ ← Dynamic
│  ├─────────────────────────────────┤ │
│  │  👍 42  ❤️ 18  😂 7             │ │ ← 40px
│  └─────────────────────────────────┘ │
│                                     │
│  ┌─────────────────────────────────┐ │
│  │  Next Post...                   │ │
│  └─────────────────────────────────┘ │
└───────────────────────────────────────┘
```

#### Optimization:
- **Virtual Scrolling**: 화면에 보이는 것만 렌더링
- **Lazy Loading**: 이미지는 뷰포트 진입 시 로딩
- **Placeholder**: 로딩 중 회색 박스 (skeleton)
- **Cache**: 스크롤 업 시 이미 본 게시물 재사용

#### Performance Targets:
- 60fps 스크롤 (저사양 기기)
- 초기 렌더: < 100ms
- 이미지 로딩: Progressive (썸네일 → 원본)

---

### 4. Image Loading Strategy (이미지 로딩 전략)

**Progressive + Placeholder**

#### Loading Sequence:
```
1. Placeholder (1KB)
   ┌─────────────────┐
   │  ▓▓▓▓▓▓▓▓▓▓▓▓  │  Gray box
   │  ▓▓▓▓▓▓▓▓▓▓▓▓  │
   └─────────────────┘
   ↓
2. Thumbnail (5-10KB)
   ┌─────────────────┐
   │  [Blurry image] │  320px WebP
   │                 │
   └─────────────────┘
   ↓
3. Full Image (50-200KB) - Only if online
   ┌─────────────────┐
   │  [Sharp image]  │  1080px WebP
   │                 │
   └─────────────────┘
```

#### Rules:
- **오프라인**: Thumbnail만 (5-10KB)
- **온라인 (< 2G)**: Thumbnail만
- **온라인 (3G+)**: Full image
- **와이파이**: Full image + 캐싱

#### UI Indicators:
- 로딩 중: 회전 아이콘 (작게)
- 오프라인: 구름 아이콘
- 실패: 재시도 버튼

---

### 5. Sync Queue Indicator (동기화 대기열)

**오프라인 작업 시각화**

#### Design:
```
┌─────────────────────────────────────┐
│  🔴 오프라인                          │
│                                     │
│  대기 중인 작업: 3개                   │
│  ┌─────────────────────────────┐   │
│  │  📤 게시물 작성 (1)           │   │
│  │  ❤️  반응 추가 (2)            │   │
│  └─────────────────────────────┘   │
│                                     │
│  온라인 연결 시 자동 동기화됩니다.       │
└─────────────────────────────────────┘
```

#### States:
- **Pending**: 작업 대기 중
- **Syncing**: 동기화 진행 중 (진행률)
- **Success**: 완료 (자동 숨김)
- **Failed**: 실패 (재시도 옵션)

---

### 6. New Post Composer (게시물 작성)

**최소한의 UI, 최대한의 효율**

#### Design:
```
┌─────────────────────────────────────┐
│  새 게시물                            │
│                                     │
│  ┌─────────────────────────────┐   │
│  │                             │   │
│  │  무슨 생각을 하고 계신가요?      │   │
│  │                             │   │
│  │                             │   │
│  └─────────────────────────────┘   │
│                                     │
│  [📷 사진]  [취소]  [게시 ✓]        │
│                                     │
│  🔴 오프라인 - 온라인 시 업로드됩니다   │
└─────────────────────────────────────┘
```

#### Features:
- **간단한 텍스트 입력** (5000자 제한)
- **이미지 첨부** (선택적)
- **오프라인 저장** (자동)
- **동기화 상태** 표시

#### Validation:
- 빈 게시물 방지
- 이미지 크기 체크 (5MB)
- 오프라인 시 대기열 추가

---

## 🌈 Color System (색상 시스템)

### Dark Mode (기본)
```
배터리 절약 + OLED 최적화
```

#### Colors:
```css
--bg-primary: #000000      /* 순수 검정 (OLED) */
--bg-secondary: #1a1a1a    /* 카드 배경 */
--text-primary: #ffffff    /* 주 텍스트 */
--text-secondary: #999999  /* 보조 텍스트 */
--accent: #00ff88          /* 액센트 (녹색) */
--error: #ff3b30           /* 에러 */
--warning: #ff9500         /* 경고 */
```

### Light Mode (선택적)
```css
--bg-primary: #ffffff
--bg-secondary: #f5f5f5
--text-primary: #000000
--text-secondary: #666666
--accent: #00cc88
```

### Status Colors:
```css
--online: #00ff88     /* 온라인 */
--offline: #ff3b30    /* 오프라인 */
--syncing: #ff9500    /* 동기화 중 */
```

---

## 📏 Spacing & Typography

### Spacing Scale:
```css
--space-xs: 4px
--space-sm: 8px
--space-md: 16px
--space-lg: 24px
--space-xl: 32px
```

### Typography:
```css
--font-xs: 12px    /* 캡션 */
--font-sm: 14px    /* 보조 텍스트 */
--font-md: 16px    /* 본문 */
--font-lg: 20px    /* 제목 */
--font-xl: 24px    /* 큰 제목 */

--line-height: 1.5
```

### Touch Targets:
```
최소 크기: 44x44pt (Apple HIG)
권장 크기: 48x48dp (Material Design)

버튼 간격: 최소 8px
```

---

## 🔄 Animation Guidelines

### 원칙: "Less is More"

#### 허용되는 애니메이션:
```css
/* 페이드 인/아웃 */
.fade-in {
  animation: fadeIn 0.2s ease-in;
}

/* 슬라이드 */
.slide-up {
  animation: slideUp 0.3s ease-out;
}

/* 스케일 */
.scale-bounce {
  animation: scaleBounce 0.15s ease-out;
}
```

#### 금지되는 애니메이션:
- ❌ 무한 반복 애니메이션
- ❌ 3D 변환 (성능 저하)
- ❌ Parallax 효과 (배터리 소모)
- ❌ 복잡한 SVG 애니메이션

#### Duration Guidelines:
```
- Instant: 0ms (상태 변경)
- Quick: 100-200ms (피드백)
- Normal: 200-300ms (전환)
- Slow: 300-500ms (특별한 경우만)
```

---

## 📱 Screen Layouts

### 1. Feed Screen (피드 화면)

```
┌─────────────────────────────────────┐
│  [Status Bar]  🟢 Online             │  ← System
├─────────────────────────────────────┤
│  LightSNS         [⚙️]               │  ← 56px Header
├─────────────────────────────────────┤
│                                     │
│  ┌─────────────────────────────┐   │
│  │  Post 1                     │   │
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │  Post 2                     │   │  ← Scrollable
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │  Post 3                     │   │
│  └─────────────────────────────┘   │
│                                     │
├─────────────────────────────────────┤
│  [🏠]  [➕]  [👤]                   │  ← 60px Nav
└─────────────────────────────────────┘
```

### 2. Profile Screen (프로필 화면)

```
┌─────────────────────────────────────┐
│  ← Back                              │  ← Header
├─────────────────────────────────────┤
│                                     │
│      [Avatar 80x80]                 │
│                                     │
│      @username                      │
│      John Doe                       │
│                                     │
│      팔로워 1.2K  •  팔로잉 345      │
│                                     │
│  ┌───────────────────────────┐     │
│  │  내 게시물 (42)            │     │
│  ├───────────────────────────┤     │
│  │  [Post Grid 2x...]        │     │
│  │                           │     │
│  └───────────────────────────┘     │
└─────────────────────────────────────┘
```

### 3. Offline Queue Screen (동기화 대기)

```
┌─────────────────────────────────────┐
│  동기화 대기 중                        │
├─────────────────────────────────────┤
│                                     │
│  🔴 오프라인 모드                     │
│                                     │
│  대기 중인 작업 (3개)                 │
│                                     │
│  ┌─────────────────────────────┐   │
│  │  📤 게시물 작성              │   │
│  │  "Hello world..."           │   │
│  │  3분 전                      │   │
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │  ❤️  반응 추가               │   │
│  │  Post by @user              │   │
│  │  1분 전                      │   │
│  └─────────────────────────────┘   │
│                                     │
│  온라인 연결 시 자동으로 동기화됩니다.  │
└─────────────────────────────────────┘
```

---

## 🎯 Interaction Patterns

### 1. Pull to Refresh (당겨서 새로고침)

```
온라인일 때만 활성화
최소 당김: 80px
최대 당김: 120px
햅틱 피드백: 새로고침 시작 시
```

### 2. Infinite Scroll (무한 스크롤)

```
하단 200px 도달 시 다음 페이지 로드
로딩 인디케이터: 작은 스피너
오프라인: 캐시된 데이터만 표시
```

### 3. Swipe Gestures (스와이프)

```
❌ 불필요한 제스처 없음
❌ 복잡한 멀티터치 없음
✅ 단순한 탭만 사용
```

---

## ⚡ Performance Budgets

### Target Metrics:
```
First Paint: < 1s
Time to Interactive: < 2s
Scroll FPS: 60fps (minimum)
Memory Usage: < 100MB
Battery per hour: < 5%
```

### Bundle Size:
```
Total App: < 15MB
JavaScript: < 2MB
Images (cached): < 20MB
Database: < 50MB
```

---

## 📊 Offline Indicators

### Visual System:
```
1. Top Status Bar
   🟢 Online  |  🔴 Offline  |  🟡 Syncing

2. Action Feedback
   "저장됨 (오프라인)"
   "온라인 시 동기화됩니다"

3. Queue Counter
   Badge: "3" (대기 중인 작업)

4. Disabled States
   비활성화된 기능 흐림 처리
   설명 툴팁 표시
```

---

## 🧪 Accessibility (접근성)

### Touch Targets:
```
최소: 44x44pt
권장: 48x48dp
간격: 8px 이상
```

### Contrast Ratios:
```
텍스트: 4.5:1 (WCAG AA)
큰 텍스트: 3:1
UI 요소: 3:1
```

### Font Scaling:
```
시스템 글꼴 크기 지원
최소: 12px
최대: 24px
```

---

## 🎨 Component Library

### Button Variants:

```css
/* Primary */
.btn-primary {
  background: var(--accent);
  color: #000;
  padding: 12px 24px;
  border-radius: 8px;
}

/* Secondary */
.btn-secondary {
  background: transparent;
  border: 1px solid var(--accent);
  color: var(--accent);
}

/* Ghost */
.btn-ghost {
  background: transparent;
  color: var(--text-primary);
}
```

### Card:

```css
.card {
  background: var(--bg-secondary);
  border-radius: 12px;
  padding: 16px;
  margin: 8px 0;
}
```

### Avatar:

```css
.avatar {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  background: var(--bg-secondary);
}
```

---

## 📱 Platform-Specific

### iOS:
- Safe Area 지원
- Haptic Feedback 사용
- Native 폰트 (SF Pro)

### Android:
- Material ripple effects
- FAB (Floating Action Button)
- Roboto 폰트

---

## 🔍 Edge Cases

### 1. No Network
```
- 모든 기능 로컬에서 작동
- 명확한 오프라인 표시
- 동기화 대기열 표시
```

### 2. Slow Network
```
- Timeout: 30s
- 재시도: 3회
- 진행 상태 표시
```

### 3. Storage Full
```
- 경고 메시지
- 캐시 정리 옵션
- 필수 데이터만 유지
```

---

## ✅ Design Checklist

### Before Implementation:
- [ ] 오프라인에서 작동하는가?
- [ ] 데이터 사용량이 최소인가?
- [ ] 터치 타겟이 충분히 큰가?
- [ ] 로딩 상태가 명확한가?
- [ ] 에러 처리가 친절한가?
- [ ] 배터리 효율적인가?
- [ ] 저사양 기기에서 테스트했는가?

---

## 🎯 Success Metrics

### UX Goals:
```
- 오프라인 사용 가능: 100%
- 평균 세션 데이터: < 100KB
- 평균 배터리 소모: < 5%/hour
- 앱 크래시율: < 0.1%
- 사용자 만족도: > 4.5/5
```

---

## 📚 References

### Best Practices:
- Apple Human Interface Guidelines
- Material Design (Mobile)
- Progressive Web App Guidelines
- Facebook Lite Design Patterns
- WhatsApp Offline-First Approach

### Testing Tools:
- Chrome DevTools (Network Throttling)
- Android Profiler
- Xcode Instruments
- Real Device Testing (저사양)

---

**마지막 업데이트**: 2025-10-28
**작성자**: ULTRATHINK Team
**버전**: 1.0.0
